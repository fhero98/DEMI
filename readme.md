## Docker Service Operations

**Note: The original data has been deleted**

### Start Service
```bash
# Start the docker-compose service
docker-compose up -d
# or
docker compose up -d
```

### View Containers
```bash
# View running containers
docker ps
```

### Stop Service
```bash
# Stop all docker-compose services
docker-compose down
# or
docker compose down
```
